# domain/

Use-cases métier. Story 1.2 : `domain/session` (open/resume + cookie opaque).

Couche appelée par `app/(client)` et `app/(bo)` — jamais l’inverse.
Server Actions métier ici (AD-4). `(client)` n’importe jamais `(bo)`.
