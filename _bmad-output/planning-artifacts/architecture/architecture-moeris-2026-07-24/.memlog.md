---
scope: Ma table (Résidence Moeris) — plateforme avis/commande mobile web + back-office V1
purpose: build-substrate
altitude: feature
topic: Architecture spine Ma table / Moeris
updated: 2026-07-24T09:33
---

- (event) Create intent: Coaching path; deliverable = spine only; workspace bound
- (constraint) Sources inherited: PRD prd-moeris-2026-07-23 (final) + UX ux-moeris-2026-07-23 (final DESIGN/EXPERIENCE)
- (constraint) PRD: V1 web via QR (pas app store); pas de paiement digital; pas de login client; contact opt-in tél OU email; BO staff authentifié; multi-sites hors scope
- (constraint) UX override: plateforme adaptable multi-support (phone/tablette/desktop), entry QR principal; light-only Citrus; BO salle unique Menu+Commandes+Service
- (direction by user) Mode Coaching; livrable spine seule (pas de deck/doc prose pour l'instant)
- (note by user) Pas d'idée sur le paradigme / découpage système — demande coaching avec propositions
- (direction by coach) Proposer 3 formes réalistes V1: monolithe modulaire 2 surfaces + 1 API (lean); SPA séparées + API; split microservices (déconseillé V1)
- (decision by user) Paradigme A: monolithe modulaire — 1 deployable; surfaces Client + BO; cœur métier partagé (menu, commande, session, avis, contact, service). Client lecture seule sur catalogue; BO seul mutateur menu. Prevents: split microservices V1; deux vérités commande.
- (direction by coach) Altitude feature; paradigm = modular monolith (layered modules inside one deployable)
- (version) next@16.2.11 verified via npm view (2026-07-24); create-next-app@16.2.11
- (direction by coach) Stack fork: lean create-next-app Next 16 App Router + TypeScript + Tailwind; data fork: Supabase (Auth staff + Postgres + Storage photos + Realtime BO) vs Neon/Postgres + Auth.js + Prisma/Drizzle (plus portable, plus glue)
- (decision by user) Stack: full Next.js front+back (create-next-app App Router); Neon PostgreSQL + Prisma; deploy cible Vercel. Reject Supabase BaaS. Prevents: backend séparé, DB hors Postgres serverless non-Vercel-friendly.
- (version) Pinned seed: next@16.2.11, prisma/@prisma/client@7.9.0, @neondatabase/serverless@1.1.0; Auth.js next-auth@4.24.15 still listed — Auth.js v5 (next-auth@5 beta/line) is the going Next pairing; confirm at implement
- (constraint) Vercel deploy: Prisma DATABASE_URL pooled + DIRECT_URL for migrations; Node runtime for Prisma (avoid Edge unless adapter-neon); no long-lived WS servers on Vercel — BO live = poll/SSE/external, pas socket maison
- (direction by coach) Next forks: Auth.js staff; mutations Server Actions; photos Vercel Blob; BO freshness poll vs Pusher/Ably
- (decision by user) BO freshness: push managé (pas poll comme primaire). Prevents: staff qui rate des commandes faute de refresh manuel; WebSocket self-hosted incompatible Vercel.
- (direction by coach) Lean provider Ably ou Pusher Channels + éventuel browser Notification API pour onglet en fond; poll reste filet de secours
- (assumption) Lean realtime provider: Pusher Channels V1 (1 site, peu de clients BO concurrents; free tier ok). Ably si besoin history/QoS plus tard. Canal scoped site+salle; publish depuis Server Action après write Neon.
- (decision by user) Auth staff BO: credentials email+mot de passe via Auth.js (NextAuth). Client jamais authentifié. Prevents: OAuth Google obligatoire; magic-link dépendant mail pendant service.
- (assumption) Auth.js v5 + Prisma adapter + sessions DB (ou JWT staff — à figer à distill); hash mots de passe via provider Credentials Auth.js; comptes staff créés/provisionnés hors self-signup public
- (decision by user) Médias plats: Vercel Blob. Prevents: Cloudinary/S3 parallèle V1; URLs hors écosystème Vercel.
- (assumption) Blob: upload authentifié BO only; client lit URLs publiques/signed; next/image pour rendu
- (decision by user) User délègue: choisir le mieux adapté pour mutations + session
- (decision by coach) Mutations métier V1 = Server Actions only (pas de REST parallèle pour menu/commande/session/avis/contact/service). Route Handlers réservés: Auth.js handlers, webhooks Pusher/Blob si besoin. Prevents: double surface d'écriture.
- (decision by coach) Session séjour: cookie httpOnly id opaque → vérité en Neon (Session). Soft mémoire appareil = cookie séparé ou même chaîne liée guest; ressaisie tél/email résout Guest en DB. Prevents: payload métier dans le cookie.
- (decision by coach) Realtime BO: Pusher Channels [ADOPTED lean] — publish post-commit Server Action; poll fallback soft. Prevents: WS self-host Vercel.
- (direction by coach) Next elicit: stockage contact PII (clair vs hash) — divergence builders + conformité
- (question by user) Peut-on stocker les contacts dans un Sheet ?
- (direction by coach) Pushback: Sheet seul casse reconnaissance 2e visite + prefs; lean Neon source de vérité + export/sync Sheet optionnel pour ops marketing
- (decision by user) Contacts: stockés en base ET dans un Sheet
- (decision by coach) Neon = source de vérité contacts/guest (clair, staff-only). Sheet = miroir sync one-way Neon→Sheet après opt-in (Google Sheets API). Lecture reconnaissance/prefs = Neon only. Prevents: dual-write concurrent; Sheet comme lookup runtime.
- (assumption) Sheet = Google Sheets V1; sync async post-commit (best-effort); échec sync loggé sans bloquer fin d'expérience client
- (decision by user) Environnements: un seul Neon pour tout (preview+prod partagent la DB)
- (decision by coach) Vercel prod+preview → même DATABASE_URL Neon V1. Revisit: brancher Neon séparée dès que tests écrasent menu/commandes réelles. Prevents: fausse impression d'isolation preview.
- (assumption) Seed/migrations Prisma prudentes; pas de reset destructif automatisé sur cette DB unique
- (decision by user) QR: table stable (?table=id fixe sur carte imprimée); Session créée au scan; Order porte tableId. Prevents: choix table in-app V1; QR session-only.
- (direction by coach) Paquet lean restant à confirmer avant distill: modules app/(client)|app/(bo)|domain|infra; statuts Order reçue|en_prep|servie; naming FR métier / code EN; staff comptes provisionnés; rôles BO plats V1
- (assumption) Wi-Fi QR hors produit (carte physique 2 QR) — pas de captive portal dans Ma table
- (event by user) User confirmed lean package + distill go
- (decision by coach) Modules: app/(client), app/(bo), domain/, infra/. Deps: UI→domain→infra; client never imports bo. Prevents: leak mutateurs menu vers client.
- (decision by coach) Order status enum: received → preparing → served (UX reçue/en prep/servie). Transitions BO only via Server Action.
- (decision by coach) Naming: code/identifiers EN; UI copy FR. Staff accounts provisioned (no public signup); BO role flat V1 salle.
- (decision by coach) Auth staff session: Auth.js v5 Credentials + Prisma adapter; session strategy database. Confirm package next-auth@5 at implement.
- (version) Auth.js: next-auth@5.0.0-beta.32 (ligne v5 encore beta sur npm 2026-07-24); pin seed + revisiter stable si sorti avant implémentation
- (version) pusher-js / pusher packages — pin exact at implement; @vercel/blob current at implement
- (event) Reconcile PRD+UX: GAPS — applying ADs for service catalog, resume R2, Terminer gate, menu freshness, contact XOR, privacy min, multi-support, BO shell
- (decision by coach) AD batch post-reconcile: ServiceRequest enum 4 types; Session.resumeStep soft banner R2; Terminer gated after order served/received per PRD; Menu client stale<1min; Contact XOR; no tracking table/heure/compagnie; responsive multi-support; BO one shell Menu|Commandes|Service; Order.tastes snapshot
- (version) Pins post-gate: @vercel/blob@2.6.1 pusher@5.3.4 pusher-js@8.6.0 @prisma/adapter-neon@7.9.0 next-auth@5.0.0-beta.32 via next-auth@beta
- (decision by coach) Gate fixes: AD-6 JWT Credentials; AD-7 event DTO; AD-9 one active session/table; AD-12 placeOrder only; AD-14 no free-text + open→done; AD-18 cart on Session; AD-19 guest upsert; AD-20 prefs 3-5
- (event) Reviewer Gate applied (lint clean; rubric/adversarial/versions findings autofixed)
- (event) spine finalized
